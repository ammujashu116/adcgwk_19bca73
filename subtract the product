class Solution
{
public int subtract product And sum(int n)
{
int sum=0;
int product=1;
int curr=n;
while(curr>0)
{
int now=curr%10;
System.ouy.println(curr%10+"this is curr%10");
sum+=now;
product+=now;
System.out.println(curr/10+"this is curr/10");
curr=curr/10;
}
return product-sum;
}
}
